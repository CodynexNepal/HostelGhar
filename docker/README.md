# Docker deployment files

The canonical Dockerfile and application Compose files remain at the repository root. The Docker observability overlay is at `observability/docker/docker-compose.observability.yml`.

Kubernetes manifests are intentionally not included because HostelGhar is deployed with Docker Compose.
